Game:
- animation
- dialogs
- game menu
- load and save
- player statistic
- intro
...


Engine:
- NOTE: SDL_RLEACCEL may seedup transparent bliting
- NOTE: SDL_WarpMouse() may be enough instead moving window

- make nice aap build

- define SYSTEM_DATA_DIR and USER_DATA_DIR in build

- make the engine independent on Name, app developer should not
  need to edit Name.h

//-----------------------------------------------------------------
DONE:
- add a font library
- register watchers for params (screen, ...)
- prepare user and system config files,
  the system one will be processed first and the user's config
  will overwrite a configs


